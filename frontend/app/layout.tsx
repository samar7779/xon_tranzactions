import './globals.css';

export const metadata = {
  title: 'Xon Tranzaksiyalar',
  description: 'Bank tranzaksiyalari monitoring tizimi',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
